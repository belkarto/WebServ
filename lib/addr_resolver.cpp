#include "webserv.hpp"

// void	addr_resolver(struct addrinfo **resolved_addr, std::string& addr, std::string& port)
// {
// 	// struct addrinfo hints;
//     // memset(&hints, 0, sizeof(struct addrinfo));
//     // hints.ai_family = AF_INET;
//     // hints.ai_socktype = SOCK_STREAM;
// 	// int status;
//     // if ((status = getaddrinfo(addr.c_str(), port.c_str(), &hints, resolved_addr)) != 0)
// 	// 	throw Server::ConfigFileParsingException("invalid address or service");
// }